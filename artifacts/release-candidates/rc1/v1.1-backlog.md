# v1.1 Backlog

## Storage

- Replace `node:sqlite` with stable `better-sqlite3` (or libsql) — TD-001.
- WAL → incremental backup / PITR.
- Multi-process access via shared-mutex or advisory locks.

## Distributed

- Stretch: replace `peer_id`-keyed anchor table with quorum consensus (Raft or Paxos).
- Cross-region replication with conflict-free replicated data types for capability_grant state.
- Lease fencing token assignment via monotonic clock + node identity.

## Security

- Replace in-memory secret backend with OS keyring (libsecret / Windows Credential Manager / macOS Keychain).
- Hardware-bound signing of Mission Record integrity digests (HSM / TPM).
- Formal verification of the dual-contender fencing protocol.

## Observability

- OTel traces per Run, Mission Record, Agent Invocation.
- Metrics surface (counters for invocations, capability-match failures, lease acquisitions).
- Structured-logging contract for all services.

## Tooling

- `git bundle --all` + `git apply --check` for offline CI.
- Standalone CLI binary (pkg / bun build) for OperatorOS Platform.
- v0.8 importer round-trip: also export OperatorOS Platform → v0.8.x for symmetric migration.

## Documentation

- Per-package README with operator-facing usage examples.
- ADR for the in-memory + SQLite coexistence choice.
- Resolve TS-X-REDEL documentation duplicate.
